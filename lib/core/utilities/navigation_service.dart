import 'package:flutter/material.dart';

/// Shared global navigator key used across app (for GoRouter or direct navigation)
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
